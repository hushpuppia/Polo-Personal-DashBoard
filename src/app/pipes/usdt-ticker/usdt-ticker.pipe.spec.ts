import { UsdtTickerPipe } from './usdt-ticker.pipe';

describe('UsdtTickerPipe', () => {
  it('create an instance', () => {
    const pipe = new UsdtTickerPipe();
    expect(pipe).toBeTruthy();
  });
});
